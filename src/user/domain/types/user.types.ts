export type UserProps = {
    name: string;
    email: string;
    password: string;
    createdAt?: Date;
}

export type Props = {
    name?: string;
    email?: string;
    password?: string;
    createdAt?: Date; 
}