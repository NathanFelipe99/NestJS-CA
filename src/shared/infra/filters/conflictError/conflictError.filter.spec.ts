import { ConflictErrorFilter } from './conflictError.filter';

describe('ConflictErrorFilter', () => {
  it('should be defined', () => {
    expect(new ConflictErrorFilter()).toBeDefined();
  });
});
